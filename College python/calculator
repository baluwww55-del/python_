def calculate_grade(mark):
    if mark >= 90:
        return "A"
    elif mark >= 80:
        return "B"
    elif mark >= 70:
        return "C"
    elif mark >= 50:
        return "Pass"
    else:
        return "Fail"

marks = int(input("Enter Marks: "))
print("Grade:", calculate_grade(marks))
